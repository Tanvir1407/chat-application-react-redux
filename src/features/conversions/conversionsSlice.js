import { createSlice } from "@reduxjs/toolkit"

const initialState = {

}

const conversionsSlice = createSlice({
    name: "auth",
    initialState,
    reducers: {
        
    }
})

export const { } = conversionsSlice.actions;
export default conversionsSlice.reducer; 